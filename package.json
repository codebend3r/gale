{
  "name": "@codebend3r/gale",
  "version": "0.2.0",
  "description": "An extremely fast CSS linter, written in Rust. Drop-in replacement for Stylelint.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/johnnybenson/gale"
  },
  "homepage": "https://github.com/johnnybenson/gale",
  "bugs": "https://github.com/johnnybenson/gale/issues",
  "keywords": [
    "css",
    "linter",
    "stylelint",
    "scss",
    "less",
    "rust",
    "fast",
    "lint",
    "gale"
  ],
  "type": "module",
  "main": "npm/index.mjs",
  "exports": {
    ".": {
      "import": "./npm/index.mjs",
      "require": "./npm/index.cjs"
    }
  },
  "bin": {
    "gale": "npm/bin/gale"
  },
  "files": [
    "npm/",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=16.0.0"
  },
  "scripts": {
    "system-check": "run-s fmt:check lint test:rust build:release test:api test:features",
    "fmt": "cargo fmt --all",
    "fmt:check": "cargo fmt --all --check",
    "lint": "cargo clippy --workspace --all-targets -- -D warnings",
    "test": "run-s test:rust test:api test:features",
    "test:rust": "cargo test --workspace",
    "test:api": "GALE_BINARY=target/release/gale node npm/test.mjs",
    "test:features": "bun test tests/features",
    "build:release": "cargo build --release",
    "build": "bash scripts/build-npm.sh",
    "build:all": "bash scripts/build-npm.sh --all"
  },
  "devDependencies": {
    "npm-run-all2": "^9.0.3"
  }
}
